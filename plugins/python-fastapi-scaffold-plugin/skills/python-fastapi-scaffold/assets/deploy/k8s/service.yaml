apiVersion: v1
kind: Service
metadata:
  name: {{PROJECT_NAME}}
  labels:
    app: {{PROJECT_NAME}}
spec:
  type: ClusterIP
  selector:
    app: {{PROJECT_NAME}}
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
